# dependencies / build output
node_modules/
dist/
build/
*.tsbuildinfo

# electron desktop packaging output
desktop/release/
desktop/build/

# staged client deliverables (.exe / .apk) — built, not source
deliverables/

# logs / env
*.log
.env
.env.local

# helper runtime data (device db, screenshots, recordings)
helper/data/
helper/recordings/
helper/screenshots/

# android
app/.gradle/
app/**/build/
app/local.properties
local.properties
# release signing — NEVER commit the keystore or its passwords
*.keystore
*.jks
keystore.properties

# os / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
